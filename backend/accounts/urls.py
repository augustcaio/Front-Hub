"""
URL configuration for accounts app.
"""
from django.urls import path
from typing import List

app_name: str = 'accounts'

urlpatterns: List = [
    # URLs will be added in future tasks (authentication endpoints)
]

