"""
URL configuration for devices app.
"""
from django.urls import path
from typing import List

app_name: str = 'devices'

urlpatterns: List = [
    # URLs will be added in future tasks (CRUD endpoints)
]

